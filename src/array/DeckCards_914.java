package array;

class DeckCards_914 {
    public boolean hasGroupsSizeX(int[] deck) {
        if (deck == null || deck.length <= 0) {
            return false;
        }

        if (deck.length == 1) {
            return false;
        }

        for (int x: deck) {

        }
    }
}